@model IEnumerable<OilProducts.Models.DeliveryCompany>

@{
    ViewBag.Title = "Index";
   // var grid = new WebGrid(Model);
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.deliveryCompanyId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.companyName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.companyEmail)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.companyPhone)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.logo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.deliveryCompanyId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.companyName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.companyEmail)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.companyPhone)
        </td>
        <td>
            <img src="@item.logo" />
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.deliveryCompanyId }) |
            @Html.ActionLink("Details", "Details", new { id=item.deliveryCompanyId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.deliveryCompanyId })
        </td>
    </tr>
}

    @*@grid.GetHtml(
    columns: grid.Columns(
    grid.Column)
    )*@



</table>
